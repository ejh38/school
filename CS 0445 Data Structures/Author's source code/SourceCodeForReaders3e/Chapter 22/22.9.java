private class TableEntry<S, T> {   private S key;   private T value;   private boolean inTable; // true if entry is in hash table   private TableEntry(S searchKey, T dataValue)   {      key = searchKey;      value = dataValue;      inTable = true;   } // end constructor   // < methods getKey, getValue, setValue, isIn, isRemoved, setToIn, and setToRemoved >   //  . . .} // end TableEntry